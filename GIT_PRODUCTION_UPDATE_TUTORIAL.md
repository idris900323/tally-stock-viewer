git ls-files logs
git ls-files *.pid
git commit -m "Stop tracking runtime/data files; enforce safe deploy ignores"
git push origin main
# Git Production Update Tutorial — pointer

This workflow guide is consolidated into the master setup document. For the complete safe-update workflow, backup steps, and commands, see:

[MASTER_SETUP.md](MASTER_SETUP.md)
